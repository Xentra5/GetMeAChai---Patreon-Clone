// Login layout — fonts are already loaded by the root app/layout.js,
// so no need to re-import them here. Keeping this minimal.
export default function LoginLayout({ children }) {
  return <>{children}</>;
}
